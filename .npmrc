# Quiet the noise that isn't actionable.
# audit-level=high: moderate findings in build tooling are not worth blocking
# an install over, and treating them as urgent trains you to ignore all of it.
audit-level=high
fund=false
